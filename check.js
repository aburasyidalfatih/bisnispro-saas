const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const tenants = await prisma.tenant.findMany({ include: { users: { include: { user: true } }, dripLogs: true } }); console.log(JSON.stringify(tenants.map(t => ({ id: t.id, name: t.name, createdAt: t.createdAt, users: t.users.length, ownerEmail: t.users[0]?.user?.email, logs: t.dripLogs.length })), null, 2)); } main().finally(() => prisma.$disconnect());
